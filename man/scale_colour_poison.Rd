% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales_functions.R
\name{scale_colour_poison}
\alias{scale_colour_poison}
\alias{scale_color_poison}
\title{Poison frog colour scale for ggplot2}
\usage{
scale_colour_poison(
  name,
  type = c("discrete", "continuous"),
  direction = 1,
  alpha = NULL,
  ...
)

scale_color_poison(
  name,
  type = c("discrete", "continuous"),
  direction = 1,
  alpha = NULL,
  ...
)
}
\arguments{
\item{name}{Character. Name of the poison-frog palette to use
(must exist in your internal \code{poison_palettes} registry).}

\item{type}{Either \code{"discrete"} or \code{"continuous"}. Selects which kind of
ggplot2 scale is constructed.}

\item{direction}{Integer. \code{1} for the palette in its stored order, \code{-1} to
reverse it.}

\item{alpha}{Optional numeric in \verb{[0, 1]}. If supplied, applies a uniform
transparency to all colours (both discrete and continuous modes).}

\item{...}{Additional arguments passed to the underlying ggplot2 scale
(e.g. \code{limits}, \code{breaks}, \code{labels}, \code{guide}, \code{oob}, etc.).}
}
\value{
A ggplot2 scale object.
}
\description{
A unified \strong{colour} scale that works with either \strong{discrete} or
\strong{continuous} data, controlled by \code{type}. For discrete data it uses
\code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}, and for continuous data it builds a smooth
gradient with \code{\link[ggplot2:scale_gradient]{ggplot2::scale_colour_gradientn()}}.
}
\details{
\itemize{
\item \strong{Discrete:} relies on an internal function factory \code{poison_pal()} that
returns \code{n} colours on demand for \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}.
\item \strong{Continuous:} generates a 256-colour gradient via \code{poison_palette()}
(type \code{"continuous"}) and passes it to \code{\link[ggplot2:scale_gradient]{ggplot2::scale_colour_gradientn()}}.
}
}
\examples{
\dontrun{
library(ggplot2)

# Discrete example
ggplot(mtcars, aes(factor(cyl), fill = factor(gear))) +
  geom_bar() +
  scale_fill_poison("O_sylvatica_N", type = "discrete")

# Continuous example
ggplot(mtcars, aes(wt, mpg, colour = disp)) +
  geom_point(size = 3) +
  scale_colour_poison("D_tinctorius", type = "continuous", direction = -1)
}

}
\seealso{
\code{\link[=scale_fill_poison]{scale_fill_poison()}}, \code{poison_palette()}, \code{poison_pal()}, \code{show_poison()}
}
